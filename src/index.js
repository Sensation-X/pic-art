window.addEventListener('DOMContentLoaded', function () {

    'use strict';

    let calc = require(''),
        form = require(''),
        slider = require(''),
        tabs = require(''),
        timer = require(''),
        modal = require('');
    
    calc();
    form();
    slider();
    tabs();
    timer();
    modal();
});